import { Address } from '../../types';

// Data that returned by the API that can be used for
// tx building.
export type MWrappedMData = {};

export type DexParams = {
  // TODO: DexParams is set of parameters the can
  // be used to initiate a DEX fork.
  // Complete me!
};
